import React from 'react';

import * as S from './styles';

const SkillCard: React.FC = (): JSX.Element => {
  return (
    <S.Container>
      <h1>SkillCard</h1>
    </S.Container>
  );
};

export default SkillCard;
